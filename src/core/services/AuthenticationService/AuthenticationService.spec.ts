describe('AuthenticationService', () => {
  describe('generateToken', () => {
    it.todo('should generate a token for a user');
  });

  describe('validateToken', () => {
    it.todo('should return a user from a valid token');
  });
});
